================================================================================
QUESTION: How are blueprints registered?

FLOW SUMMARY
The registration of blueprints involves a series of function calls starting from the `register_blueprint` function, which is responsible for registering a blueprint on the application. This function calls the `register` function, which in turn creates a `BlueprintSetupState` object using the `make_setup_state` function and then calls the `_merge_blueprint_funcs` function to merge the blueprint functions. The `BlueprintSetupState` object is used to pass the application and options to the register callback functions. The `make_setup_state` function is also called by the `Blueprint` class, and the `BlueprintSetupState` class is used by the `Blueprint` class to register the blueprint with the application.

STEP-BY-STEP TRACE
1. The `register_blueprint` function is called to register a blueprint on the application, which is defined in `flask_repo\src\flask\sansio\blueprints.py` at lines 256-271.
2. The `register_blueprint` function calls the `register` function, which is defined in `flask_repo\src\flask\sansio\blueprints.py` at lines 273-377.
3. The `register` function calls the `make_setup_state` function to create a `BlueprintSetupState` object, which is defined in `flask_repo\src\flask\sansio\blueprints.py` at lines 246-253.
4. The `make_setup_state` function returns a `BlueprintSetupState` object, which is then used by the `register` function to call the register callback functions.
5. The `register` function also calls the `_merge_blueprint_funcs` function to merge the blueprint functions, which is defined in `flask_repo\src\flask\sansio\blueprints.py` at lines 379-410.

FILES INVOLVED
- `flask_repo\src\flask\sansio\blueprints.py`: This file contains the `register_blueprint`, `register`, `make_setup_state`, and `_merge_blueprint_funcs` functions, as well as the `Blueprint` and `BlueprintSetupState` classes.
- `flask_repo\src\flask\sansio\scaffold.py`: This file contains the `_endpoint_from_view_func` function, which is called by the `BlueprintSetupState` class.

KEY RELATIONSHIPS
- The `register_blueprint` function calls the `register` function to register the blueprint.
- The `register` function calls the `make_setup_state` function to create a `BlueprintSetupState` object.
- The `register` function calls the `_merge_blueprint_funcs` function to merge the blueprint functions.
- The `make_setup_state` function is also called by the `Blueprint` class to create a `BlueprintSetupState` object.
- The `BlueprintSetupState` class is used by the `Blueprint` class to register the blueprint with the application.

